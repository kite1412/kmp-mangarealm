<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/mr_logo_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mr_logo_icon"
        android:supportsRtl="true"
        android:theme="@style/LaunchTheme">
<!--        android:theme="@android:style/Theme.Material.Light.NoActionBar">-->

        <!--Place under <activity>        -->
        <!--            android:screenOrientation="portrait"-->
        <activity
            android:exported="true"
            android:configChanges="screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>